	comecei executando todos os aplicativos necessarios, airflow, kafka, producer, celery, redis, minio, rabbitmq, api flask, streamlit. Estarei utilizando a abordagem 2, utilizando o airflow organizar, coordenar e monitorar a execução do pipeline de dados. Criei um novo arquivo python com o exemplo DAG que o professor disponibilizou na pasta dos dags do airflow. Escrevi um dockerfile para a instalação das bibliotecas python necessárias para o programa, que foram kafka-python, redis, pandas, e pika e fiz um novo build do aplicativo. 

	Após isso, tentei rodar o código no airflow, porém o primeiro estagio estava dando erro pois não estava conseguindo conectar ao minio. Percebi que foi porque os aplicativos não estavam conectados à rede do airflow, então não tinha como conectar eles. Após essa correção, tentei no ambiente Jupyter para ver se estava funcionando e dessa vez funcionou. Também tinha testado a conexão com o minio através da biblioteca minio-py. Depois, tive alguns problemas com a conexão ao kafka, no qual era um erro de configuração do endereço da API do kafka. Após todas as configurações, o pipeline conseguiu executar todas as tarefas no arquivo python e enviou um alerta para o rabbitmq.

	Fiz um código em shell para construir e executar todos os ambientes automaticamente, e também inserir o dataset da qualidade do ar no minio, para apenas precisar executar o pipeline no Airflow e analisar os resultados. Após isso, é necessário fazer o aplicativo do Streamlit. Montei um dashboard simples mostrando um aviso caso um dado possua NO2 > 160 ou CO > 6, e também um gráfico da distribuição de avisos em comparação com os dados comuns. Vale notar também que o Redis possui um limite de 200 dados, portanto não consegui demonstrar a quantidade correta dos dados utilizados com o Streamlit e o Redis.